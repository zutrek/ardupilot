#include "AC_DroneShowManager.h"

bool AC_DroneShowManager::_run_debug_request_handler(const mavlink_command_int_t &packet)
{
    // Use this function to add ad-hoc debugging code to the firmware as needed.
    // Make sure to return true if you actually use the handler; otherwise return
    // false.
    return true;
}
